# Watch me reinvent the wheel to make my electrons reason
---

So.. machines learn. but how?

### Initially...
- `/primitive` contains all the intuitions I had and implemented by only have a vague sense of what a `Neuron` is, and what `weights` and `bias` meant.
- This uses "blame division"(as I like to call it) to just divide the blame linearly based on the proportion of the input of the neuron.
- "Heuristics" is what I was doing.
- Machines can't learn via heuristics. (well logically it's a very unsound way of propogating blame backward through a network)

